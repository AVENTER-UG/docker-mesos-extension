import type { ReactNode } from 'react';
interface Props {
    readonly disableDarkTheme?: boolean;
    readonly children: ReactNode;
}
export declare function DockerMuiThemeProvider({ disableDarkTheme, children }: Props): JSX.Element;
export {};
